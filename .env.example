# Database Configuration
MYSQL_ROOT_PASSWORD=root123!
MYSQL_DATABASE=flik_db
MYSQL_USER=flik_user
MYSQL_PASSWORD=flik_password

# Redis Configuration (Optional - defaults work for most cases)
# SPRING_DATA_REDIS_HOST=redis
# SPRING_DATA_REDIS_PORT=6379

# Application Configuration
# SERVER_PORT=8080
# JAVA_OPTS=-Xms256m -Xmx1g -XX:+UseG1GC

# Build and Deployment (for CI/CD)
# BUILD_TAG=latest
# DOCKER_REGISTRY=ghcr.io
# DOCKER_REPO=your-repo/flik-server